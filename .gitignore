# Real config files, e.g. config.yaml, config-claude.yaml -- never the
# .example one, which is what gets tracked.
config*.yaml
!config.example.yaml

# Generated by the wizards -- real per-target data, secrets, and local
# staging, none of which belongs in this repo.
targets.conf
staging/

.venv/
venv/
__pycache__/
*.pyc
.pytest_cache/
*.egg-info/
dist/
build/
.mypy_cache/
.ruff_cache/
*.config.yaml
.mcp.json
